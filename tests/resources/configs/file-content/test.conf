cert: include content(file(pushcorn.com.crt))
key:  include file(content(pushcorn.com.key))
number:  include file(content(number))
string:  include file(content(string))
boolean:  include file(content(boolean))
null:  include file(content(null))
